//\\  ----> OBJECTS IN JS <---- //\\

/*


let a = {
    name: 'Jhon',
    age: 20,
    gender: 'male',
    address: 'New York',
    hobbies: ['reading', 'writing', 'coding'], 
    university: 'PQR University'
    }

console.log(a.hobbies[2]);  //for access obj details
console.log(a["hobbies"]);  //for access obj details
a.age = 21;                 // to change obj details
delete a.university;        // to delete obj details
console.log(a);


//\\  ----> OBJECTS METHODS JS <---- //\\


Object.freeze(obj)      ====>  Prevents any modification to an object and its properties.
Object.entries(obj)     ====>  Returns an array of [key, value] pairs for all own properties of an object.
Object.keys(obj)        ====>  Returns an array of the object's own property names.
Object.values(obj)      ====>  Returns an array of the object's own property values.
Object.assign(obj)      ====>  Copies all own enumerable properties from one or more source objects to a target object.
Object.create(obj)      ====>  Creates a new object, using an existing object as the prototype of the new object.

console.log(Object.keys(a)); 
console.log(Object.values(a)); 
console.log(Object.entries(a));
console.log(Object.assign(a));
console.log(Object.freeze(a)); 


*/
